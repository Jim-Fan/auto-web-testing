To build, execute below commands in project root:

    npm install

    npm install –g npx

    npm install --save-dev chromedriver

    npm install --save-dev @wdio/cli


To run, execute:

    npx wdio wdio.conf.js
